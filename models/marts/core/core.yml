
version: 2

models:
    - name: dim_customers
      description: "table of customers"
      columns:
        - name: customer_id
          description: "primary key"
          tests:
            - unique
            - not_null